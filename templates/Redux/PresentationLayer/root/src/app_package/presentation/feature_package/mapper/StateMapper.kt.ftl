package ${packageName}.presentation.${feature_name}.mapper

import ${packageName}.presentation.${feature_name}.model.${listItemName}
import ${packageName}.presentation.${feature_name}.${viewName}
import javax.inject.Inject

class ${stateMapperName}
@Inject
constructor() {
    fun mapState(state: ${viewName}.State): ${viewName}.State =
        TODO()
}